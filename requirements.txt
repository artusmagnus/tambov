# Runtime dependencies for mask annotation and wetness mapping tools.
# OpenCV provides video I/O, GUI windows, drawing, and FFmpeg-backed RTSP capture.
opencv-python>=4.8,<5
# NumPy is used for mask storage, temporal averaging, and OKLab math.
numpy>=1.24,<3
